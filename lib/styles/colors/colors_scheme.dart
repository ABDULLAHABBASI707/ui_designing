import 'package:flutter/material.dart';
import 'custom_colors.dart';

class CustomColorScheme{
  static ColorScheme light=const ColorScheme.light(primary: CustomColors.black300);
  static ColorScheme dark=const ColorScheme.dark(primary:CustomColors.white50);
}